# 딩코딩코 Chapter 1 - 알고리즘과 친해지기(1),(2)

# [Python] 최댓값(Max) & 최빈값(Mode) 직접 구현 정리

## 1. 오늘 한 것

- 반복문으로 **최댓값(Max)** 찾기 구현
- `dict` 카운팅으로 **최빈값(Mode)** 찾기 구현
    - **대소문자 구분 X**
    - **공백/기호 제외(알파벳만 카운트)**

---

## 2. 핵심 개념

### 2.1 최댓값(Max) 찾기

- 첫 값을 `max_val`로 잡고
- 리스트를 한 번 순회하면서 더 큰 값이 나오면 갱신한다.

**시간복잡도:** `O(n)`

**공간복잡도:** `O(1)`

---

### 2.2 최빈값(Mode) 찾기

- `dict`로 각 알파벳이 몇 번 나왔는지 센다.
- 카운팅 패턴: `counts[ch] = counts.get(ch, 0) + 1`
- 카운팅이 끝나면 `counts.items()`를 순회하면서 최대 빈도 key를 찾는다.

**시간복잡도:** `O(n)`

**공간복잡도:** `O(k)` (k = 고유 알파벳 개수)

---

## 3. 느낀 점 / 배운 점

- 이중 반복으로도 풀 수 있지만 데이터가 커지면 `O(n^2)`라 비효율적이다.
- 최댓값/최빈값은 결국 **“후보를 두고 갱신한다”** 라는 동일한 흐름으로 해결된다.
- 최빈값은 코드보다 먼저 **전처리 규칙(무엇을 세는가)**를 정하는 게 중요하다.
    - 예: 대소문자 통일, 공백/기호 제외, 알파벳만 카운트

---

## 4. 구현 코드

### 4.1 최댓값(Max) 찾기

```python
def find_max_num(arr):
    if not arr:
        return None

    max_val = arr[0]
    for x in arr[1:]:
        if x > max_val:
            max_val = x

    return max_val
```

**4.2 최빈값(Mode) 찾기 (알파벳만 / 대소문자 구분 X / 공백·기호 제외)**

```python
def most_common_char(s: str):
    if not s:
        return None

    counts = {}

    for ch in s:
        if ch.isalpha():          # 공백/기호 제외
            ch = ch.lower()       # 대소문자 통일
            counts[ch] = counts.get(ch, 0) + 1

    if not counts:                # 알파벳이 하나도 없는 경우
        return None

    max_char = None
    max_count = 0

    for ch, cnt in counts.items():
        if cnt > max_count:
            max_char = ch
            max_count = cnt

    return max_char, max_count
```

---

4.3 테스트

```python
print(find_max_num([3, 1, 5, 2]))          # 5
print(most_common_char("Hello World!!"))   # ('l', 3)
print(most_common_char("123!!!"))          # None
```

## **요약**

- **Max:** 한 번 순회하며 최댓값 갱신 → O(n)
- **Mode:** dict로 카운팅 후 최대 빈도 찾기 → O(n)
- **Mode는 전처리 규칙이 핵심** (대소문자 통일 / 공백·기호 제외 등)

---

# 개념을 정리하자면!

### **✅ 핵심 개념 정리**

### **1) 최댓값 찾기**

- *브루트포스(이중 반복)**로도 가능하지만 시간복잡도가 O(n^2)라 비효율적
- 보통은 **한 번 순회**하면서 갱신하는 방식이 정석 → O(n)
- 파이썬 내장함수 max()가 있지만, 직접 구현해보면서 “갱신 로직”을 체감함

### **2) 최빈값 찾기**

- 핵심은 **카운팅 자료구조(dict)** 사용
- counts[ch] = counts.get(ch, 0) + 1 패턴이 가장 자주 쓰임
- 이후 counts를 돌면서 최댓값을 가진 key를 찾으면 최빈값
- (옵션) 대소문자 통일, 공백/기호 제외 등 “전처리”가 결과에 큰 영향을 줌

### **💡 오늘 느낀 점 / 배운 점**

- 단순히 max()/Counter 쓰는 것보다, **왜 그 결과가 나오는지**를 직접 구현하면서 이해가 빨라졌음
- 이중 반복으로 풀면 직관적이지만, 데이터가 커지면 느려진다는 걸 **시간복잡도로 연결**해서 이해함
- 최빈값은 “정답 자체”보다 **전처리 규칙(무엇을 세는가)**를 먼저 정의하는 게 중요하다는 걸 느낌

### **🔍 헷갈렸던 점 & 해결**

- “최댓값 후보”를 왜 업데이트하는지 → 기준값을 두고 비교하며 갱신하는 흐름으로 이해
- 최빈값 동률 처리(같은 횟수일 때) → 정책을 정해야 함
    - 예: 먼저 나온 글자 우선 / 알파벳순 우선 / 모두 반환 등

### **🔧 내가 정리한 구현 포인트(재사용할 패턴)**

- 최댓값 패턴
    - max_val을 두고 순회하면서 if x > max_val: max_val = x
- 카운팅 패턴
    - dict.get(key, 0) + 1
- 최댓 카운트 찾기
    - for key, cnt in counts.items():에서 갱신

###