# 딩코딩코 Chapter 1 - 알고리즘과 친해지기 (2)

## **반복되지 않는 문자 (First Non-Repeating Character)**

### **✅ 문제**

- 문자열에서 **등장 횟수가 1번인 문자들** 중
    
    **문자열에서 가장 먼저 등장한 문자**를 반환
    
- 없으면 _ 반환

예)

- "abadabac" → 비반복 문자: d, c → 문자열에서 먼저 나온 건 d → d

---

### **✅ “첫 번째” 기준**

“첫 번째”는 2가지 해석이 가능함

1. **문자열 등장 순서 기준**: 왼쪽부터 봤을 때 처음 나오는 비반복 문자
2. **알파벳 순서 기준**: a~z 중 알파벳 순으로 가장 앞선 비반복 문자

 이 문제는 **문자열 등장 순서 기준**으로 풂

---

### 💡 **핵심 아이디어**

1. int[26] 배열로 알파벳 등장 횟수(count) 저장
2. 문자열을 다시 왼쪽부터 훑으면서 count == 1인 문자를 처음 만나면 반환
3. 끝까지 없으면 _

---

### ⏱️ **시간 복잡도**

- 문자열 2번 순회 → O(N)
- 알파벳 배열은 26칸(상수) → 무시 가능
    
    ➡️ **최종: O(N)**
    

---

### **⚠️ 주의사항**

- 이 방식은 사실상 **영어 알파벳(a~z)** 기준으로 안전함
- 대문자/기호가 섞이면:
    - toLowerCase() 적용
    - 'a' ~ 'z' 범위만 처리하는 방식이 안전

---

## 최종코드

```java
class FindNotRepeatingFirstCharacter {
    public static char findNotRepeatingFirstCharacter(String string) {
        int[] count = new int[26];

        // 1) 빈도수 카운트
        for (char ch : string.toCharArray()) {
            ch = Character.toLowerCase(ch);
            if (ch < 'a' || ch > 'z') continue;
            count[ch - 'a']++;
        }

        // 2) 문자열 등장 순서 기준으로 첫 비반복 문자 찾기
        for (char ch : string.toCharArray()) {
            ch = Character.toLowerCase(ch);
            if (ch < 'a' || ch > 'z') continue;
            if (count[ch - 'a'] == 1) return ch;
        }

        return '_';
    }

    public static void main(String[] args) {
        System.out.println("정답 = d 현재 풀이 값 = " + findNotRepeatingFirstCharacter("abadabac"));
        System.out.println("정답 = c 현재 풀이 값 = " + findNotRepeatingFirstCharacter("aabbcddd"));
        System.out.println("정답 = _ 현재 풀이 값 = " + findNotRepeatingFirstCharacter("aaaaaaaa"));
    }
}
```